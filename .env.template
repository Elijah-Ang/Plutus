# Copy to .env (gitignored) only if Keychain is not used. Never commit real values.
ALPACA_API_KEY=replace_with_paper_key
ALPACA_SECRET_KEY=replace_with_paper_secret
TELEGRAM_BOT_TOKEN=replace_with_bot_token
TELEGRAM_CHAT_ID=replace_with_chat_id
TELEGRAM_ALLOWED_USER_ID=replace_with_numeric_user_id
OPENAI_API_KEY=replace_with_api_key
# Live confirmation is intentionally absent. TradingAgent v1 is paper-only.
