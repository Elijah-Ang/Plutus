# ARCHIVED CONFIGURATION (unused by runtime as of plutus_effective_config_v2).
# The documented and validated runtime source is config/config.yaml under the
# strategies section. Keep this file only as a historical reference until the
# compatibility cleanup removes it.
rule_based_v2:
  enabled: true
  maximum_volatility_20d: 0.45
  require_ma_200: true
  stop_drawdown_pct: 0.08
ml_shadow:
  enabled: false
  horizon_periods: 5
  estimated_cost_rate: 0.001
  models: [logistic_regression, random_forest]
